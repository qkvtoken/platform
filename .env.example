# Anthropic — primary LLM
ANTHROPIC_API_KEY=sk-ant-...

# Default Claude model (override per-app if needed)
ANTHROPIC_MODEL=claude-sonnet-4-6

# Postgres + pgvector (matches docker-compose.yml)
DATABASE_URL=postgresql://qkv:qkv@localhost:5432/qkv
